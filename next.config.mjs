/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "apetechs.com",
      "uploadthing.com",
      "firebasestorage.googleapis.com",
    ],
  },
};

export default nextConfig;
